export(
    bsFolds,
    classify,
    classifyPairs,
    cluster,
    contributions,
    cvFolds,
    input,
    imputeCensored,
    misclassificationPenalties,
    normalize,
    parscores,
    perfScatterPlot,
    predTable,
    print.llama.data,
    print.llama.model,
    regression,
    regressionPairs,
    singleBest,
    singleBestByCount,
    singleBestByPar,
    singleBestBySuccesses,
    successes,
    trainTest,
    vbs
)
S3method(print, llama.data)
S3method(print, llama.model)
importFrom(rJava, J, .jpackage, .jarray)
importFrom(parallelMap, parallelMap, parallelRegisterLevels)
import(ggplot2)
import(mlr)
import(checkmate)
